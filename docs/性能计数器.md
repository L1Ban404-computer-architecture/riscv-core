# 性能计数器

`performance_stats` 只观察流水线握手、各级 payload 已有的原始指令和核心侧 CoreBus 的
monitor interface。计数不参与功能控制；复位清零，累计值按各自位宽自然回绕。
I-cache 使用平行的 `icache_performance_stats`，只观察 cache-CPU CoreBus，不并入核心
payload。mini-soc 与 ysyx-soc 从各自快照生成 `*-perf.log`，终端仍只打印 IPC。

## 指令与局部阻塞

类别数组顺序固定为 compute、load、store、branch、jump、system、other，
对应 `riscv_core_pkg::perf_class_e`。分类完全位于 `performance_stats`，功能
流水级和 decoder 不生成或传递性能类别字段，也不提供专用分类端口。
观察器统一读取各级原始指令的 `instr[6:0]`，按主 opcode 分组：

- OP、OP-IMM、LUI、AUIPC → compute。
- LOAD、STORE → load、store。
- BRANCH → branch；JAL、JALR → jump。
- SYSTEM、MISC-MEM → system（含 CSR、ECALL、EBREAK、MRET、FENCE/FENCE.I）。
- 未知 opcode、有效的 `EXC_INST_ACCESS_FAULT` 取指访问异常 → other。

不额外校验 funct3/funct7 等子编码。未支持的 M 扩展 OP 编码归 compute，非法
LOAD 子编码归 load；other 不代表所有非法指令。执行和数据访问异常不覆盖
opcode 分类，取指异常仅在 exception.valid 有效时覆盖分类。

ID、EX、MEM、WB 分别观察 IF/ID、ID/EX、EX/MEM、MEM/WB 的原始指令和异常；
退休分类使用 MEM/WB。同一组合分类函数供全部统计点复用。
局部阻塞为“入口 valid 且不 ready，同时出口没有阻塞”，WB 仅看入口；
redirect/flush 沿用原有排除条件。不同阶段局部阻塞可能重叠，不可直接相加
解释为 CPI 损失。当前 WB 无背压，因此 WB 阻塞为零是正常结果。

IF 还没有分类，只累计后端愿意接收但 IF/ID 无有效事务的周期；临时单指令
模式主动停止取指的后端执行周期不计入。没有边界 fire 或统一 stall 计数器。

退休数量沿用调试提交握手口径，包含异常，并非架构 `minstret`。
总体平均局部阻塞 = 各类别阻塞之和 / 总退休数量；分类平均局部阻塞 =
该类阻塞 / 该类退休数量。不进行错误路径的单独归因或回滚。

## CoreBus 访存统计

imem/dmem 在核心侧观察，imem 包含最终丢弃的取指，dmem 包含 MMIO。
`memory_performance_stats` 使用 `core_bus_if.monitor` 和性能模块共享的周期
计数器，只采集握手事件，不修改 ready/valid，不限制功能通路发出请求。

每个端口保留五个字段：64 位事务数 `transaction_count`、128 位请求/响应
握手时刻之和 `request_cycle_sum` / `response_cycle_sum`，以及 64 位请求/响应
背压周期数。事务数仅在请求握手时累加；两个通道独立更新，不维护配对队列、
完成数、最后请求时刻或在途状态，也不再检查单请求在途限制。

仿真端计算：

```text
平均延迟     = (响应时刻之和 - 请求时刻之和) / 事务数
请求背压占比 = 请求背压周期数 / 总周期数 × 100%
响应背压占比 = 响应背压周期数 / 总周期数 × 100%
```

时间戳使用共享的 64 位周期计数器，扩展至 128 位后累加。仿真端先用 128 位
整数计算差值的绝对值，再转换为浮点并保留符号，避免大累计量相减的精度损失。
统计窗口内周期计数器不应回绕。请求与响应全部配对后，平均延迟对应请求握手
到响应握手的平均周期数：同拍 0、下一拍 1，包含响应背压，错误响应也累计。
不再修正停止时未完成的请求，此时延迟结果可能为负，不代表已完成事务均值。
请求/响应背压分别按对应通道 valid 且不 ready 累计。redirect 不清除统计。

## I-cache 统计

`icache_performance_stats` 在 cache 内观察 CoreBus 请求/响应握手，自备 64 位周期
计数器作时间戳。阻塞式、单 outstanding 的 I-cache 上，同拍请求与响应记为命中，
仅请求或仅响应分别累计缺失时刻。字段为请求数 `request_count`、命中数
`hit_count`，以及命中/缺失各自的 128 位请求、响应时刻之和。缺失次数由
`request_count - hit_count` 得到。不维护配对队列，不修正停止时未完成的缺失。

仿真端计算：

```text
命中率         = 命中次数 / 请求次数
命中平均延迟   = (命中响应时刻之和 - 命中请求时刻之和) / 命中次数
缺失平均延迟   = (缺失响应时刻之和 - 缺失请求时刻之和) / 缺失次数
```

延迟口径与 CoreBus 访存统计相同：同拍 0、下一拍 1，包含响应背压。当前命中路径
是同拍原子握手，因此命中延迟为 0。mini-soc 不实例化 I-cache，也不采样这些字段。

## 日志布局

首行显示模型名、IPC，以及原始 `instret` 与 `cycle` 整数。IF 局部阻塞均值放在
`Local stall (cyc/inst)` 标题的下一行。流水线表把总体 `all` 和分类合并，列为
share、ID、EX、MEM、WB；省略退休数量为零的类别，阶段列固定。
总体按全部退休指令归一，类别按本类退休指令归一，均值单位为 cyc/inst。
访存表仅有 imem/dmem 两行，报告 latency(cyc)、req stall(%) 和 rsp stall(%)。
ysyx-soc 额外输出 Cache 表，报告 icache 的 hit rate、hit lat(cyc) 和
miss lat(cyc)；mini-soc 无 cache，不打印该段。

数字使用四位有效数字，零显示 `0`，零分母显示 `-`；首行 `instret` 与 `cycle` 为十进制
整数。日志不再展开 CPI、吞吐率、在途/忙碌信息或 definitions。终端保留 IPC 输出，日志路径
仍为 `*-perf.log`。

运行 AM 工作负载后，可在模型的 `*-perf.log` 中检查上述统计和格式；
端到端运行命令见工作台根目录 README 的“构建与端到端验证”。
